# 🕺 Shuffle Battle Game `v1.0.0`

**Shuffle Battle** — это динамичная браузерная ритм-игра, объединяющая мощь игрового движка Phaser 3 и современный веб-интерфейс на Next.js 15. Стань мастером шаффла, комбинируй Running Man и T-Step, взламывай счетчики очков и докажи MC, что твой флоу — лучший в системе.

---

## 🚀 Стек технологий

Проект построен на передовом стеке 2026 года:

- **Framework:** [Next.js 15.3](https://nextjs.org/) (App Router) & [React 19](react.dev)
- **Game Engine:** [Phaser 3.90](https://phaser.io) — отрисовка сцен, физика движений и обработка спрайтов.
- **Styling:** [Tailwind CSS 4.1](https://tailwindcss.com) — использование JIT-движка нового поколения.
- **Animations:** [Framer Motion 12](https://www.framer.com/) — интерактивные меню и плавные UI-переходы.

---

## 🎮 Игровые механики

- **Shuffle ID:** Система идентификации игрока в глобальной сети баттлов.
- **Combo System:** Связывай движения в цепочки (RM → T-Step) для получения очков и множителей.
- **Virtual Gamepad (Touch-управление):** Полностью кастомизированные кнопки действий для мобильных устройств. 
- **Интерактивный MC:** Живой ведущий, который реагирует на твою технику и подбадривает (или троллит) в зависимости от результата.
- **Условие Вектора:** Для зачета комбо необходимо сменить направление движения (WASD) хотя бы один раз внутри связки.

---

## 🛠 Установка и запуск

1.  **Клонирование репозитория:**

    ```bash
    git clone git@github.com:Galdenysh/shuffle-battle-game.git
    cd shuffle-battle-game
    ```

2.  **Установка зависимостей:**

    ```bash
    npm install
    ```

3.  **Запуск в режиме разработки:**

    ```bash
    npm run dev
    ```

    _Игра будет доступна по адресу: `http://localhost:8080`_

4.  **Сборка для продакшена:**
    ```bash
    npm run build
    ```

---

## 📜 Доступные скрипты

- `npm run dev` — запуск Next.js на порту 8080 с предварительным выполнением `log.js`.
- `npm run build` — полная оптимизированная сборка проекта.
- `npm run dev-nolog` — запуск среды разработки без кастомного логгера.
- `npm run build-nolog` — сборка проекта без логгера.

---

## 📁 Структура проекта

- `/app` — Страницы с регистрацией и игрой.
- `/components` — React UI-компоненты (меню, модалки, кнопки).
- `/game` — игровая логика Phaser (сцены, менеджеры сущностей, анимации).
- `/hooks` — вспомогательные кастомные хуки.
- `/lib` — вспомогательные утилиты для работы со стилями и конфигами.
- `/types` — TypeScript интерфейсы и перечисления (Abilities, Combos).

---

## 👤 Автор

**Igor Petrov**

- GitHub: [@Galdenysh](https://github.com/Galdenysh)

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Подробности в файле [LICENSE](https://opensource.org/).
