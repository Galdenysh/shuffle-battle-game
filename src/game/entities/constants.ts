export const BACKGROUND_ANIMATION_DEFAULTS = {
  frameRate: 12,
  repeat: -1,
} as const;

export const CHARACTER_ANIMATION_DEFAULTS = {
  start: 0,
  end: 5,
  zeroPad: 3,
  frameRate: 12,
  repeat: -1,
} as const;
